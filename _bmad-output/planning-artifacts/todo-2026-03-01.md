- Select a row/column
- Insert an empty row/column before the selected one
- Selected multiple consecutive cells, e.g. to merge them
- Edit/add/delete styles
- Context menu (when right clicking a cell)
- Fold the top two "rows" into one: 3 icons, file saved indicator, current cell indicator, formula bar.
- Hover, e.g. when the full error text does not fit into the cell.
- Bug: when scrolling down and reaching the bottom of the sheet, rows are added (correctly) but the display resets to the top.
- Bug: "=" on its own should be illegal
- Use an initial "'" (quote) to quote text, even if it is numeric. Similar to same feature in Excel.
- Bug: merge should refuse to work if more than 1 cell is non-empty.
- Alignment: support left/right/center.
- Merge should result in centralized text.
- (Provided this is enabled by grid element): allow right-to-left spreadsheets, e.g. for RTL languages (Hebrew).

